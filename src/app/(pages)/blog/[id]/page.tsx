import BlogDetailPage from '@/app/features/user/blog/blogDetail/blogDetail';

export default function BlogDetail({ params }: { params: { id: string } }) {
	return (
		<>
			<BlogDetailPage />
		</>
	);
}
